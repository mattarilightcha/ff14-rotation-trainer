# SPEC — FF14 スキル回し練習アプリ 仕様書

- 文書ステータス: Draft v0.1（設計のみ・未実装）
- 関連文書: [GAME_DATA.md](./GAME_DATA.md) / [INPUT_HUD.md](./INPUT_HUD.md) / [STATE_MACHINE.md](./STATE_MACHINE.md) / [DATA_SCHEMA.md](./DATA_SCHEMA.md) / [TEST_PLAN.md](./TEST_PLAN.md) / [SCREENS.md](./SCREENS.md) / [DEV_PLAN.md](./DEV_PLAN.md) / [DEPLOY.md](./DEPLOY.md)

> **重要な方針**
> 本書および関連文書では、ゲーム内のスキル名・リキャスト・効果時間・威力・ゲージ量などの**ゲーム仕様を推測で埋めない**。
> 未確認の項目はすべて `TODO(GAME-xx)` として明示し、[§10 未確認ゲーム仕様一覧](#10-未確認ゲーム仕様一覧todo) に集約する。
>
> **v0.2 追記:** 名称・アイコン・詠唱・リキャスト・コンボ・置き換え先などは、FF14 クライアントから抽出したデータ（第 1 層）で埋まるようになった。抽出データにない値（ゲージ量・効果時間・置き換え条件など）だけを、出典付きの補足データ（第 2 層）で持つ。詳細は [GAME_DATA.md](./GAME_DATA.md)。
> アプリ本体（エンジン・採点・UI）はゲーム仕様に依存しない汎用設計とし、ジョブ固有の値はデータファイルからのみ読み込む。

---

## 1. 目的

| # | 目的 | 本アプリでの実現方法 |
|---|------|--------------------|
| 1 | 実際の戦闘を模したタイムライン上でスキルを入力する | 仮想時計で進むシミュレーション上で、ボタン/キーボードからアクションを入力する |
| 2 | 正しい順番だけでなく、GCD 停止・アビリティの挟みすぎ・バフずれを判定する | 入力ごとに判定エンジンが「ミス種別＋理由」を記録し、終了後に採点する |
| 3 | 初心者が開幕回しを反復練習できる | ガイド練習モード（次のスキルを提示）とワンボタンのリトライ |
| 4 | スマートフォンと PC の両方で使える | レスポンシブ UI、タップ入力とキーボード入力の両対応 |

## 2. スコープ

### 2.1 初期リリース（MVP）の対象

| 項目 | 値 |
|------|----|
| ジョブ | 侍（Samurai） |
| レベル | 100 |
| 戦闘時間 | 120 秒（データ上は可変。MVP の既定値が 120 秒） |
| 敵 | 単体木人のみ（常に射程内・常に正面/側面/背面の区別なし ※方向指定は TODO(GAME-20)） |
| モード | ガイド練習 / 自由練習 |

### 2.2 非スコープ（MVP ではやらない）

- 他ジョブ（データ構造は多ジョブ前提で設計するが、データは侍のみ。モックではナイト・白魔道士を試作済み: §11）
- 複数敵・範囲攻撃の命中数・ギミック・移動・ダウンタイム
- ダメージ量（DPS）の算出（威力値の検証コストが高いため。将来拡張 §11）
- パーティバフ・シナジー
- クリティカル/ダイレクトヒット等の乱数
- バックエンド、ログイン、外部 API、データベース
- 音声の同梱（アイコン画像はクライアントから抽出したものを使う。[GAME_DATA.md §6](./GAME_DATA.md#6-アイコンと権利表記)）

## 3. 技術構成と制約

| 分類 | 採用 | 備考 |
|------|------|------|
| UI | React + TypeScript | 関数コンポーネント + hooks |
| ビルド | Vite | 公開パスに合わせて `base` を環境変数 `VITE_BASE` で指定（既定案 `/tools/ff14-rotation-trainer/`） |
| テスト | Vitest（+ @testing-library/react, jsdom） | エンジンは React 非依存の純粋 TS として単体テスト |
| ルーティング | ハッシュルーティング（`#/...`） | サーバーのリライト設定が不要で、ブログ本体の URL 設定と衝突しないため |
| 保存 | `localStorage` のみ | スキーマバージョン付き（[DATA_SCHEMA.md §6](./DATA_SCHEMA.md#6-localstorage-スキーマ)） |
| 公開 | ブログ **nettoge.com** の VPS に静的ファイルとして設置。main へのマージで GitHub Actions が SSH + rsync でデプロイ | 詳細は [DEPLOY.md](./DEPLOY.md)。GitHub Pages は使わない |
| アイコン | クライアントから抽出したアイコン（`public/icons/`）。ない場合だけ文字 + CSS の仮アイコン | 配信するのは対象ジョブで使う分だけ。権利表記は GAME_DATA §6、利用条件の確認は LEGAL-01 |
| ゲームデータ | 第 1 層: クライアント抽出データ（`src/data/ffxiv/`、自動生成）＋ 第 2 層: 補足データ（`src/data/jobs/`、手書き・出典付き）をビルド時に合成 | [GAME_DATA.md](./GAME_DATA.md) |
| 外部通信 | なし | フォントも同梱またはシステムフォント。広告・解析の扱いは TODO(DEPLOY-04) |
| ブログとの関係 | 独立した HTML ページとして動作。記事からはリンク、または同一オリジンの iframe（`?embed=1`）で埋め込む | ブログの CSS/JS の影響を受けない |

### 3.1 権利表記

- 画面フッターと README に「非公式ファンツールであり、スクウェア・エニックス社とは無関係」である旨を表示する。
- 著作権表記の文面は TODO(LEGAL-01)：スクウェア・エニックスの著作物利用許諾条件を確認して決定する。

## 4. 用語

| 用語 | 定義（本アプリ内） |
|------|------------------|
| GCD | ウェポンスキル等、共通のリキャストタイマー（グローバルクールダウン）を共有するアクション |
| oGCD / アビリティ | GCD を共有しないアクション。GCD の合間に挟む（ウィーブ） |
| リキャスト | アクションを再使用できるまでの時間 |
| チャージ | 複数回ストックできるアクションの残り回数 |
| アニメーションロック | アクション使用後、次のアクションを受け付けない時間 |
| 詠唱 | アクション発動までの入力不可時間（移動は MVP 非対象） |
| ウィーブ | GCD の合間に oGCD を使うこと |
| クリップ | oGCD のアニメーションロックや詠唱により、次の GCD の開始が「GCD が回った時刻」より遅れること |
| GCD 停止（アイドル） | GCD が使用可能なのに何も GCD を押していない時間 |
| コンボ | 直前の特定アクションを条件に効果が変わる GCD の連続 |
| コンボ切れ | コンボ継続条件を満たさずに次段を使った / コンボ受付時間が切れた / 別の GCD で上書きした |
| 仮想時計 | シミュレーション内の時刻（ミリ秒、整数）。t=0 が戦闘開始 |
| お手本（ガイドシーケンス） | 期待される入力の列。開幕回しデータから生成 |
| 入力ログ | `{ t, actionId }` の配列。これを再生すれば同じ結果が再現できる |

## 5. モード

### 5.1 ガイド練習モード

- 常に「次に押すべきスキル」をハイライト表示し、その次の 2〜3 手も小さく表示する。
- 誤ったスキルを押した場合の挙動は設定で選択:
  - **ブロック**（既定）: 誤入力はシミュレーションに反映せず、理由を表示して正しい入力を待つ。時間は進み続ける。
  - **続行**: 誤入力もシミュレーションに反映し、ガイドは再同期（§8.2）を試みる。
- お手本は開幕回しデータ（`OpenerDefinition`）の終わりまで。以降は「お手本なし」で自由入力となる（ループ回しのデータ化は TODO(GAME-40)）。
- 任意設定:「ゆっくりモード」（仮想時計の速度倍率 0.5x / 0.75x / 1.0x）。採点結果には速度倍率を明記する。

### 5.2 自由練習モード

- お手本の提示なし（非表示）。入力は全てシミュレーションに反映。
- ミス理由はリアルタイムに小さく表示（設定でオフ可）。
- 終了後、選択した開幕回しとのずれ・各採点項目を表示する。

### 5.3 共通

- 開始前に 3 秒カウントダウン（§6-1）。
- 途中終了（中断）とリトライ（同条件で即再開始）ボタン。
- 戦闘時間到達で自動終了し結果画面へ。

## 6. 機能要件

各機能の詳細な状態遷移は [STATE_MACHINE.md](./STATE_MACHINE.md) を参照。

| # | 機能 | 要件 | 備考 / TODO |
|---|------|------|------------|
| 1 | 3 秒カウントダウン | 「3・2・1・開始」を表示し、0 到達で t=0（戦闘開始）。カウント中の入力はプリプル入力として扱う | カウントダウン中に使ってよいアクション（プリプル）の有無・条件は TODO(GAME-30)。データ側で `prepullAllowed` を持つ |
| 2 | 経過時間と GCD 表示 | 経過時間（mm:ss.s）と残り時間。GCD の回転を円形/バーで表示。アニメーションロック・詠唱も表示 | GCD 基本値・スキルスピード補正・ヘイスト効果は TODO(GAME-01〜03) |
| 3 | スキルボタン・キーボード・ゲームパッド操作 | 画面上ボタン（タップ/クリック）、キーボード（修飾キー付きの割り当て可）、ゲームパッド（Gamepad API、クロスホットバー）。割り当ては変更可能で localStorage に保存。連打によるキュー入力（先行入力）に対応 | 先行入力の決まりは §6.1（値は仮: GAME-05） |
| 4 | コンボ状態 | 現在のコンボ段階と受付残り時間を表示。コンボ可能なボタンを強調 | コンボ受付時間は TODO(GAME-06) |
| 5 | リキャストとチャージ | 各ボタンにリキャスト残り（数値+暗転スイープ）とチャージ数を表示 | 各アクションの値は TODO(GAME-10) |
| 6 | ジョブゲージ | データ定義に従う汎用ゲージ表示（数値ゲージ / スタック / フラグ集合） | 侍のゲージ構成は TODO(GAME-12〜14) |
| 7 | バフとデバフの残り時間 | 自己バフ・敵デバフ（DoT 含む）をアイコン+残り秒で表示。切れそうなものを警告色 | 効果時間は TODO(GAME-11) |
| 8 | 入力履歴 | 時刻・アクション・判定（OK / 警告 / ミス）を時系列リスト＋タイムライン図で表示 | |
| 9 | お手本となる次のスキル表示 | ガイドモードで次の 1 手を強調、以降 2〜3 手をプレビュー | |
| 10 | ミスした理由の表示 | 入力直後にトースト/インライン表示。理由コードと日本語メッセージ（§7.2） | |
| 11 | 終了後の採点 | 採点項目（§7）ごとの点数・内訳・該当箇所（タイムラインへジャンプ） | |
| 12 | 共有 | 練習設定（と任意で入力ログ）を URL または JSON ファイルで共有・読込 | 形式は [DATA_SCHEMA.md §5](./DATA_SCHEMA.md#5-共有フォーマット) |
| 13 | 設定ファイルの読み込み | 手元の FF14 設定ファイル（ホットバー・キー割り当て・HUD 配置）をブラウザ内で解析して取り込む。サーバーへ送らない | [INPUT_HUD.md §5](./INPUT_HUD.md#5-設定ファイルの読み込み)。形式は TODO(CFG-01〜05) |
| 14 | HUD 配置のクローン | 取り込んだ HUD 配置のとおりに、ホットバー・ゲージ・バフ・キャストバーを並べて表示（PC のみ） | [INPUT_HUD.md §6](./INPUT_HUD.md#6-hud-配置のクローン) |

### 6.1 入力仕様（詳細）

- 1 回の入力 = `{ actionId, source: 'pointer' | 'keyboard', realTime, virtualTime }`。
- 入力が「今すぐ実行可能」ならその時刻に実行。
- 実行できなくても、**そのアクション自身のリキャストの残りが 0.5 秒以下**なら **キュー**（先行入力）し、実行可能になった瞬間に実行する（GAME-05）。
  - 比べるのは、GCD なら GCD の残り、固有のリキャストがある技はその残り（チャージ制は次のチャージまで）。アニメーション硬直と詠唱の残りは条件に入れない。硬直中・詠唱中に押したアビリティは、硬直・詠唱が明けた瞬間に出る。
  - キューは 1 件だけ。**先に押したものが優先**で、キューに入っている間の入力は無視する（同じボタンの押し直しも同じ）。GCD とアビリティを同時にキューすることはできない。
  - キューした入力は、実行する時点でもう一度使えるかを判定し、使えなければ拒否して捨てる（例: その間にコンボが切れた・射程の外に出た）。
  - 地面指定のアクション（抽出データの `targetArea`）はキューしない。
  - 出典（定説。0.5 秒という数値の公式の記述はない）: ゲーム本来のキュー処理を再現したプラグインのコード（ReAction の `QueueAdjustments.cs` の `queueThreshold = 0.5`）、クライアントの解析資料（FFXIVClientStructs の `ActionManager`: キューの欄が 1 組だけ）、キューの入れ替えがプラグインの追加機能として作られていること（ゲーム本来は入れ替えない）、プレイヤーの検証記事。
- それ以外は **拒否** し、拒否理由（リキャスト中、条件未達 等）を記録する。拒否入力は採点で「無効入力」として数える（減点はしない。§7 の設定で変更可）。
- キーボード: `keydown` のみ処理し、`repeat`（押しっぱなし）は無視する。押しっぱなしでは連打にならない（実機の挙動は不確か: GAME-05）。
- スマートフォン: `pointerdown` で即時発火（`click` の遅延を避ける）。ダブルタップズーム抑止（`touch-action: manipulation`）。

### 6.1a 光る条件（ハイライト）

ホットバーのアクションが光る（点線の枠が回る）条件は、**ゲームデータに基づく 2 つだけ**とする。推測で条件を足さない。

| 条件 | ゲームデータ | 侍の例 |
|------|-------------|--------|
| コンボ | Action の `ActionCombo`（抽出データの `comboFrom`）。直前のコンボ技を受付時間内に使っていれば、その次の技が光る | 暁風の後の陣風・士風・雪風、陣風の後の月光 |
| 発動条件のステータス | Action の `ActionProcStatus`（抽出データの `actionProcStatus`、指すステータスは `actionProcStatusId`）。そのステータスが付いている間光る | 燕飛（燕飛効果アップ）、返し五剣・返し雪月花・天道返し五剣・天道返し雪月花（燕返し実行可）、奥義波切（奥義波切実行可）、残心（残心実行可） |

- 上のどちらにも当たらないもの（居合術の変化先、照破、明鏡止水中のコンボ技、返し波切）は光らせない。明鏡止水中に特別な光り方があるかはゲーム内で要確認（GAME-18）。
- 使えない状態（剣気不足など）の暗い表示と、光る表示は別に扱う（両方同時に付くことがある）。

### 6.2 時間モデル

- シミュレーションは **仮想時計（整数ミリ秒）** による決定的（deterministic）処理とする。
- UI は `requestAnimationFrame` で実時間差分を仮想時計に変換して `tick(now)` を呼ぶだけ。ロジックは持たない。
- 同じ「練習設定 + 入力ログ」からは常に同じ結果が再現される（テスト容易性と共有のため）。
- タブが非アクティブになった場合は自動で一時停止する（`visibilitychange`）。一時停止があった試行は結果に明記する。

### 6.3 通信遅延・アニメーションロックの扱い

- アニメーションロック（硬直）の長さはサーバーが送ってくる値で、クライアントのデータにない（GAME-04）。コミュニティの解析（定説）から、次の値を仮に置く。
  - 詠唱のない技: **0.6 秒＋応答の遅れ**。応答の遅れ（サーバーまでの往復と処理）は設定で変えられる（既定 50ms。0 / 50 / 100 / 150ms）。
  - 詠唱のある技: 詠唱中は硬直なし。詠唱完了の後に **0.1 秒**（応答の遅れは足さない）。
  - 例外: 侍の必殺剣・夜天（後ろへ飛び退く）は 0.8 秒。必殺剣・暁天は 0.6 秒と 0.8 秒の説があり、0.6 秒にしている。
  - 出典: xiv.dev の animation-lock の解説、ffxiv_bossmod の `AnimationLockTweak.cs`・`ActionDefinition.cs`（詠唱後 `CastAnimLock 0.1`、夜天 `animLock=0.800`）。
- 実際のゲームは「使えるようになった次のフレーム」で出るため、フレームレートが低いほど遅れる。練習ではフレームの遅れを入れない。

### 6.4 詠唱と滑り撃ち

- 詠唱の残りが **0.5 秒より多い**うちに動く（ジャンプを含む）と、詠唱は中断される。中断すると効果は出ず、GCD のリキャストは戻る（すぐ次を使える）。
- 残りが 0.5 秒を切ってからは、動いても詠唱は完了する（**滑り撃ち**）。効果（与ダメージ上昇の計算）はこの時点で決まり、以後は戦闘不能になっても完了する。ダメージの表示は詠唱完了の時点。
  - 理由: サーバーは詠唱終了の約 0.5 秒前に行動を確定して結果を送り、それを受け取った後は詠唱を取り消せない（FFXIVClientStructs の `CastInfo`）。
  - 猶予の長さは 0.5 秒ちょうどではなく「0.46＋0.02×詠唱時間」という実測もある（xivintheshell）。練習では 0.5 秒にする（GAME-55）。
- 移動中に詠唱のある技を押すと、詠唱は始まり、すぐに中断される。「移動中は詠唱できません」のようなエラー文は見つからず、「始まってから中断される」という記述があるため（不確か: GAME-55）。
- GCD のリキャストは詠唱の開始から回る。詠唱時間がリキャスト以上の技（レイズなど）は、次の GCD が詠唱完了＋0.1 秒の後になる（キャスターの 0.1 秒）。
- 侍の居合術と奥義波切の詠唱は、Lv100 では 1.3 秒（Lv74 の特性「居合術効果アップ」。風花では短くならない）。抽出データの詠唱時間（1.8 秒）は特性の前の値。特性の説明文は未抽出のため仮（GAME-64）。

## 7. 採点

### 7.1 採点項目

各項目は 0〜100 点で算出し、重み付き平均を総合点とする。重みは `ScoringProfile`（データファイル）で定義。
全ての判定は入力ログの再シミュレーション結果（イベント列）から算出する。

| 項目 ID | 項目 | 定義 | 算出式（案） |
|---------|------|------|-------------|
| `gcdUptime` | GCD 稼働率 | 戦闘時間のうち、GCD がリキャスト中・詠唱中だった時間の割合 | `1 - idleMs / evaluatedMs`。`idleMs` = GCD 使用可能になってから次の GCD を開始するまでの合計。最初の GCD 前の時間の扱いは設定（既定: t=0 から計測） |
| `sequence` | 正しいスキル順 | お手本シーケンスとの一致度 | 実行済みアクション列とお手本列の最長共通部分列（LCS）長 / お手本長。GCD と oGCD は別に計算しても表示する |
| `comboBreaks` | コンボ切れ | コンボ継続に失敗した回数 | `max(0, 100 - 回数 × penalty)`。原因（時間切れ / 別 GCD で上書き / 前段なし）を内訳表示 |
| `clipping` | アビリティ挟みすぎによるクリップ | oGCD のロックにより GCD 開始が遅れた合計時間と回数 | `clipMs` = Σ max(0, 次GCD開始 - GCD使用可能時刻)（その区間で oGCD/ロックが原因のもの）。点数は `clipMs` に対する閾値テーブルで算出 |
| `statusUptime` | バフ・DoT の維持 | 維持対象ステータス（データで指定）の稼働率と、切らした回数・早すぎる上書き回数 | 対象ごとに `activeMs / (evaluatedMs - 付与可能になるまでの時間)`。早期上書きは残り時間の無駄（ms）として別集計 |
| `overcap` | リソースあふれ | ゲージ上限超過量・チャージが最大のまま放置した時間・同じフラグの重複付与 | ゲージ: 捨てた量の合計。チャージ: 最大チャージ到達後の経過時間合計 |
| `keyActions` | 主要アクションの使用回数 | データで指定した主要アクションの使用回数 vs 期待回数 | `min(1, 実回数/期待回数)` の平均。期待回数はデータで明示するか、リキャストから理論最大回数を算出（TODO(GAME-41)：理論最大の算出条件） |
| `openerDeviation` | 開幕回しからのずれ | 開幕回しの各ステップの順序ずれと時刻ずれ | 順序: 最初に一致しなくなったステップ番号。時刻: 各ステップの `実時刻 - お手本時刻` の絶対値平均（お手本時刻は理想シミュレーションから算出） |

> 判定しきい値（何 ms 以上をクリップとみなすか、トリプルウィーブ判定など）は **ゲーム仕様ではなくアプリの採点ポリシー** であり、`ScoringProfile` に初期値を置いて調整可能にする。初期値は実装時に決め、根拠を記載する。

### 7.2 ミス理由コード（リアルタイム表示）

| コード | 表示例 | 検出タイミング |
|--------|--------|---------------|
| `ON_COOLDOWN` | 「○○はリキャスト中です（残り 12.3 秒）」 | 入力拒否時 |
| `NO_CHARGES` | 「○○のチャージがありません」 | 入力拒否時 |
| `CONDITION_UNMET` | 「○○の使用条件（××）を満たしていません」 | 入力拒否時 |
| `INSUFFICIENT_RESOURCE` | 「ゲージが足りません（必要 25 / 現在 10）」 | 入力拒否時 |
| `NOT_READY` | 「このアクションはまだ使用できません（GCD の残り 0.9 秒。先行入力は残り 0.5 秒から）」 | キュー不可の拒否時（詠唱中でないとき） |
| `CASTING` | 「詠唱中のため使用できません（GCD の残り 2.3 秒。先行入力は残り 0.5 秒から）」 | キュー不可の拒否時（詠唱中） |
| `QUEUE_IGNORED` | 「先行入力中（陣風）のため、士風は無視されました」 | キューに入っている間の入力（1 回だけ表示） |
| `COMBO_BROKEN` | 「コンボが途切れました（前段なし）」 | 実行時 |
| `COMBO_EXPIRED` | 「コンボの受付時間が切れました」 | tick 時 |
| `GCD_IDLE` | 「GCD が 0.8 秒止まりました」 | 次 GCD 実行時 |
| `CLIPPED` | 「アビリティの挟みすぎで GCD が 0.3 秒遅れました」 | 次 GCD 実行時 |
| `STATUS_DROPPED` | 「××が切れました」 | tick 時 |
| `STATUS_EARLY_REFRESH` | 「××を早めに更新しました（残り 8 秒を無駄に）」 | 実行時 |
| `RESOURCE_OVERCAP` | 「ゲージが 10 あふれました」 | 実行時 |
| `CHARGES_CAPPED` | 「○○のチャージが最大のままです」 | tick 時（一定時間ごとに 1 回） |
| `WRONG_ACTION` | 「お手本は ○○ です」 | ガイドモードの入力時 |

- ゲームの文言は「このアクションはまだ使用できません。」「詠唱中のため使用できません。」（公式 UI ガイド）。練習用に、残り時間と先行入力の目安を付け足す。「アニメーション硬直中です」という文言はゲームにないので使わない。

### 7.3 結果画面に出す情報

- 総合点・評価ランク（ランク閾値は `ScoringProfile`）
- 項目別点数と内訳、該当箇所へのリンク（タイムライン上のマーカー）
- 前回・自己ベストとの比較（localStorage の履歴から）
- 「この結果を共有」「リトライ」「設定を変えてリトライ」

## 8. ガイドシーケンス

### 8.1 生成

- `OpenerDefinition` の `steps`（アクション ID の列、任意でプリプル時刻指定）をそのままお手本とする。
- 各ステップの **お手本時刻** は、同じエンジンで「最速入力（使用可能になった瞬間に押す）」をシミュレーションして算出する（手入力しない）。

### 8.2 再同期（続行モード・自由モードのずれ計算）

- 実行されたアクションがお手本の現在ステップと異なる場合、先読みウィンドウ（既定 3 手）内に一致するステップがあればそこへ進める。無ければステップを進めない。
- 採点の `sequence` は再同期ではなく LCS で算出するため、再同期ロジックの影響を受けない。

## 9. 非機能要件

| 分類 | 要件 |
|------|------|
| 対応ブラウザ | 最新版の Chrome / Edge / Safari（iOS 含む）/ Firefox |
| 画面幅 | 360px〜（スマートフォン縦）/ 768px〜（タブレット）/ 1024px〜（PC） |
| 入力遅延 | 入力イベントから画面反映まで 1 フレーム以内を目標（`pointerdown` / `keydown` を直接処理） |
| 時間精度 | 判定は仮想時計の整数 ms。`performance.now()` を使用 |
| オフライン | 初回ロード後はネットワーク不要（外部リソースを読まない） |
| アクセシビリティ | ボタンに `aria-label`、色だけに依存しない判定表示（記号併用）、`prefers-reduced-motion` 対応 |
| 保存容量 | localStorage 合計 1MB 以内を目標。履歴は件数上限で古いものから削除。ブログと同じオリジンのため、キーは `ff14rt:` 接頭辞のものだけを扱う |
| データ分離 | ゲームデータは `src/data/` 以下の JSON / TS データファイルのみ。エンジン・UI コードにアクション名や数値を書かない（lint ルール or テストで検出） |

## 10. 未確認ゲーム仕様一覧（TODO）

以下は **公式情報（ジョブガイド・パッチノート等）で確認してからデータ化する**。確認前は値を入れず、データファイル上も `verified: false` とする。
確認済みになったら、出典（URL・パッチバージョン・確認日）を `DataSource` に記録する（[DATA_SCHEMA.md §2.7](./DATA_SCHEMA.md#27-出典と検証状態)）。

### 10.1 共通システム

| ID | 項目 | 備考 |
|----|------|------|
| GAME-01 | GCD の基本リキャスト時間 | ✅ 第 1 層で解決（`recast` 2.5 秒、補正前） |
| GAME-02 | スキルスピードによる GCD 短縮の計算式と、Lv100 で想定するスキルスピード値 | 練習では「GCD 値を直接指定」でも可とする方針案。要判断 |
| GAME-03 | ヘイスト系バフの効果量と計算式（侍の自己ヘイストの有無・量を含む） | 量は説明文から第 2 層へ（風花）。計算式（切り捨て単位など）は未確認 |
| GAME-04 | アニメーションロックの標準値（通常 / 詠唱あり / 例外アクション） | モックは仮の値: 詠唱なし 0.6 秒＋応答の遅れ（設定、既定 50ms）・詠唱の後 0.1 秒・夜天 0.8 秒（§6.3）。サーバーの値でクライアントのデータにないため、出典は解析資料（定説） |
| GAME-05 | 先行入力（キュー）受付時間 | モックは仮の値: そのアクション自身のリキャストの残り 0.5 秒・1 件・先勝ち・地面指定は不可・マクロのアクションは不可（§6.1。定説）。押しっぱなしで連打にならないかは不確か |
| GAME-06 | コンボ受付時間 | モックは仮の値 30 秒（定説。パッチノートでは未確認） |
| GAME-07 | 詠唱のあるアクションの詠唱時間と、詠唱がスキルスピード/ヘイストの影響を受けるか | 詠唱時間は ✅ 第 1 層（`cast`）。影響の有無は説明文（風花「キャストタイム…短縮」）から第 2 層へ |
| GAME-08 | リキャストがスキルスピード/ヘイストの影響を受けるアクションの範囲 | |
| GAME-09 | バフ・デバフの「付与タイミング」（使用時か、命中時か。遅延の有無） | MVP は使用時即時付与で近似する案。要判断 |

### 10.2 侍 Lv100 のアクション・ステータス

| ID | 項目 | 備考 |
|----|------|------|
| GAME-10 | Lv100 時点で使用可能なアクションの一覧（名称・種別 GCD/oGCD・リキャスト・チャージ数・詠唱時間・使用条件・効果） | ✅ 一覧・名称・GCD 種別・詠唱・リキャスト・上位版は第 1 層で解決。特性で変わるチャージ数（明鏡止水など）と使用条件・効果は第 2 層 |
| GAME-11 | 各バフ・デバフ（DoT 含む）の名称・効果時間・スタック・重複時の挙動（上書き/延長/上限） | 効果時間は説明文から第 2 層へ。ステータス行の抽出漏れは DATA-01。重複時の挙動は未確認 |
| GAME-12 | ジョブゲージの構成（ゲージ名・上限・増減するアクションと量） | 増減量は説明文から第 2 層へ。コスト type の対応は DATA-02。上限は未確認 |
| GAME-13 | 「閃」相当のフラグ型リソースの種類・付与条件・消費条件 | 名称含め要確認 |
| GAME-14 | スタック型リソースの有無・上限・増減条件 | |
| GAME-15 | 居合術・返し系など「直前アクションや状態で使えるアクション」の条件と、アイコン置き換え挙動 | 置き換え先は ✅ 第 1 層（`replacesAction`）。条件は説明文から第 2 層へ |
| GAME-16 | 特定アクションによる「コンボ段階を満たした扱い」になる効果（コンボ省略効果）の仕様 | |
| GAME-17 | 威力値 | MVP では使用しない（DPS 算出は非スコープ） |
| GAME-18 | 明鏡止水中にコンボ技が光るか（ゲームデータの ActionProcStatus には指定がない） | ゲーム内で確認 |
| GAME-20 | 方向指定の有無と、木人練習で方向を扱うか | 有無は説明文（「背面攻撃時威力」「側面攻撃時威力」）から第 2 層へ。モックは見下ろしの練習場で背面・側面を判定する（角度は GAME-53） |

### 10.2a ナイト・白魔道士（モックで仮に置いた決まり）

説明文（抽出データの description）から読めない決まり。`mock/jobs.js` に「仮」と書いて置いている。

| ID | 項目 | モックの仮の値 | 備考 |
|----|------|---------------|------|
| GAME-60 | レクイエスカットのスタックが減る技 | 魔法（種類 2）とコンフィテオル以降の技を使うたびに 1 つ減る | 説明文には「魔法を詠唱時間無しで実行できる」「威力を上昇させる」としかない |
| GAME-61 | オウスゲージの増え方 | 戦闘中 2.5 秒ごとに 5 増える（オートアタックの代わり） | 実機はオートアタックの命中ごとに増える（特性の説明文が未抽出） |
| GAME-62 | ヒーリングリリーの増え方 | 戦闘中 20 秒ごとに 1 つ（上限 3） | 特性の説明文が未抽出。3 つのまま時間が来たら「あふれ」として数える |
| GAME-63 | コンボを切らない技どうしのコンボ（コンフィテオル → ブレード・オブ・フェイス → トゥルース → ヴァラー） | 通常のコンボとは別に数え、どちらも途切れない | 抽出データでは、どれも `preservesCombo`（コンボを切らない）で、コンボ条件（`ActionCombo`）を持つ。ゲームがどう両立させているかは未確認 |
| GAME-64 | 居合術・奥義波切の Lv100 の詠唱時間 | 1.3 秒（風花で短くならない） | Lv74 の特性「居合術効果アップ」。Trait シートを抽出していないため仮。抽出データの `cast` は 1.8 秒 |
| GAME-66 | マクロの動き | 待ちのない行は上から順に試し、最初に使えたもので決まる。`/wait N`・`<wait.N>` は次の行までの待ち（1 回 60 秒まで）。実行中に別のマクロを押すと前のマクロは止まる。`<mo>` はパーティリストのマウスを乗せている行 | ゲーム内で確かめる（とくに実行中のマクロを別のマクロで止める動き、小数の待ち）。形式は CONFIG_FORMAT §9 |

### 10.3 回し・練習コンテンツ

| ID | 項目 | 備考 |
|----|------|------|
| GAME-30 | プリプル（カウントダウン中）に使用するアクションと推奨タイミング | |
| GAME-31 | 標準的な開幕回し（Lv100・単体）のアクション順 | 出典を明記。複数流派がある場合は複数データとして持つ |
| GAME-40 | 開幕後のループ回し（2 分周期等）の定義 | MVP 以降 |
| GAME-41 | 主要アクションとその「120 秒での期待使用回数」 | 理論値計算に使う条件を含む |

### 10.3a 移動・位置・練習場（3D の斜め見下ろし。モックで仮に置いた値）

モック（`mock/arena.js` の `POL`、`mock/mock.js` の `POLICY`、`mock/stages.js`）で使っている、ゲームデータにない値。実機で確かめて直す。

| ID | 項目 | モックの仮の値 | 備考 |
|----|------|---------------|------|
| GAME-50 | 移動速度 | 6 m/秒 | 歩き・走りの区別なし |
| GAME-51 | 敵（木人）の当たり判定の半径 | 3 m | ターゲットサークルの大きさ。敵ごとに違う |
| GAME-52 | 近接の射程（抽出データの射程 `-1`） | 当たり判定の外側から 3 m | 射程が正の値の技は、その m を当たり判定の外側から測る（推定） |
| GAME-53 | 方向指定の角度 | 正面 ±45°、背面 ±45°（真後ろ基準）、その間が側面 | トゥルーノース中は向きを問わない（説明文） |
| GAME-54 | 自分中心の範囲技が敵に当たる条件、当たらなかったとき（空振り）のコンボと剣気 | 範囲（`effectRange`）に当たり判定の円が入れば命中。空振りは効果なし・コンボ終了 | |
| GAME-55 | 詠唱中に動いたときの中断の条件と、中断したときの GCD | 残り 0.5 秒より前に動く（ジャンプを含む）と中断。中断すると GCD のリキャストは戻る。残り 0.5 秒を切ったら動いても完了（滑り撃ち）し、効果はその時点で決まる（§6.4） | 移動中に押すと、詠唱は始まってすぐ中断される（不確か）。猶予は「0.46＋0.02×詠唱時間」という実測もある |
| GAME-56 | 継続ダメージの間隔 | 3 秒（付けた時点から。付けたときの与ダメージ上昇で計算） | サーバーの 3 秒周期との関係は未確認 |
| GAME-57 | 移動の操作方式（スタンダード / レガシー）とカメラ | レガシーに近い動き: 押した方向へ走ってそちらを向く。「前」はカメラの向き（画面の奥）。カメラはマウスのドラッグ・KEYBIND.DAT のカメラ操作・右スティックで回す。マウスの左右同時押しで前進 | スタンダード（左右キーで旋回・後ろキーで後ずさり）は未対応。どちらの方式かは設定ファイルから未確認（CFG-15） |
| DESIGN-04 | 練習場のステージ | 練習用に作った場所（夜の稽古場: 円形・半径 20m / 昼の闘技場: 四角・40m 四方 / 夕暮れの遺跡: 円形・半径 22m）。ゲームの特定の場所ではない | 形と広さは、動ける範囲・範囲攻撃の切れ目・タンクのよけ先に効く。フィールドマーカーは「A が北、1 が北東」の並びで、中心から縁までの 6 割の所 |
| DESIGN-05 | カメラの初期値と速さ | 北が上・見下ろし 52°・距離 30m・視野角 34°。傾き 10〜86°、距離 7〜60m。マウス 1px = 0.33°（左右）・0.26°（上下）、キー 137°/秒・45°/秒 | FF14 のカメラの値ではなく、モックの操作感。設定で速さと反転を変えられる |
| DESIGN-03 | ダメージの計算 | 説明文の威力 × 与ダメージ上昇（説明文の「与ダメージを N％上昇」）× 係数（設定、既定 1）。クリティカル・ダイレクトヒット・ダメージの揺れは入れない | 敵の体力を決めると、0 で撃破して終わる |
| DESIGN-02 | 敵の範囲攻撃・タンクの動き | ゲームの特定の敵の技ではなく、練習用に作ったもの（円・ドーナツ・扇・直線・半面・足元の連続円、向きの変更・移動） | 種で並びを固定。難しさは 4 段階。タンクの腕前は 3 段階（上手: すぐよけて敵の向きを変えない / ふつう / 下手: よけるのが遅く、向きがふらつく） |
| DESIGN-06 | 役割ごとの練習の作り（ナイト・白魔道士） | ナイト: 敵はあなたを狙い、2.8 秒ごとの通常攻撃で HP が 4% 減る。相方のヒーラー（白魔道士）が、HP 90% 未満なら 3 秒ごとに 8% 回復する。白魔道士: 相方のタンク（ナイト）が敵を引きつけ、通常攻撃で 6% ずつ減る（自然には戻らない）。回復量は「回復力 100 = 最大 HP の 4%」に換算する（ケアル 20%・ケアルラ 32%）。バリアも同じ換算（ディヴァインベニゾン 500 = 20%）。MP は入れていない | 敵の攻撃力・回復量はゲームの値ではなく練習用。回復役の採点は、方向指定の枠で「タンクの守り」（戦闘不能 1 回 −40 点、HP 30% 未満の時間の割合 ×2）を見る |
| DESIGN-07 | 相方のタンクが倒れたとき | レイズ（白魔道士）で HP 50% で起きる。起こさなくても 20 秒で自分で起き上がる。倒れている間、敵はあなたを狙う | 練習を続けるための仮の動き（ゲームでは蘇生が要る。衰弱などは入れない） |
| DESIGN-08 | 回復役の練習の全体攻撃 | 白魔道士のとき、よけられない全体攻撃（詠唱 3 秒）が 15 秒目から 23〜29 秒ごとに来る。あなたの HP を 18%、相方のタンクの HP を 20% 減らす（被ダメージ軽減・バリアが効く）。被弾の数には入れない | 練習用に作ったもの（DESIGN-02 と同じ）。リタージー・オブ・ベルは「自身がダメージを受けると」鳴るため、鳴る場面を作る |
| DESIGN-09 | 設置型の技（地面指定）の置き場所と見た目 | ゲームと同じく、押すとマウスの位置にターゲットサークル（範囲の大きさ。射程 30m の外は赤）が出て、クリックした所に置く。もう一度押してもマウスの位置に置く。右クリック・Esc でやめる。パッドから押したときは自動（アサイラム = 自分と相方の間、ベル = 自分の横 2.6m）。範囲はアサイラム 15m（抽出データの `effectRange`）、ベル 20m（説明文）。見た目はゲーム内の画面とアイコンを参考に作った光の演出: ベル = 水色のガラスの線の大きなハート（下で交差）・中央の白く光る花・ガラス玉のような鈴の花（残りのスタックの数だけ。鳴るたびに 1 つ弾ける）、アサイラム = 青白い透明なドーム（縁が光り、上の方に青から橙の揺らめき） | パッドでの置き方（ゲームではスティックで動かす）は未対応。ゲームの演出（VFX）のデータは抽出していない |
| GAME-65 | ナイトのホーリースピリット・ホーリーサークルが光る条件 | 神聖魔法効果アップかレクイエスカットの間光る（ゲーム内で光ることの報告から。抽出データの ActionProcStatus には指定がない） | 再抽出で ActionProcStatus を確かめる（DATA-04） |

### 10.4 その他

| ID | 項目 | 備考 |
|----|------|------|
| LEGAL-01 | 著作権・非公式表記の文面 | スクウェア・エニックスのガイドライン確認 |
| DESIGN-01 | クリップ判定しきい値・点数換算テーブルの初期値 | ゲーム仕様ではなくアプリのポリシー |
| CFG-01〜07 | 設定ファイルの形式、パッドの両押し挙動、実物ファイルの扱い | [INPUT_HUD.md §7](./INPUT_HUD.md#7-未確定事項) |
| DATA-01〜03 | 抽出対象の拡張（ステータス）、コスト type の対応表、生成物の扱い | [GAME_DATA.md §7](./GAME_DATA.md#7-未確定事項本書由来) |
| DEPLOY-01〜05 | 公開パス・Web サーバー種別・SSH 制限・広告/解析・ステージング | [DEPLOY.md §8](./DEPLOY.md#8-未確定事項) |
| UI-01〜04 | ジョブゲージ（抽出した ULD で組み立てる）の動き・ラップ方式・クリッピングマスク・数字の字形 | [GAME_DATA.md §7](./GAME_DATA.md#7-未確定事項本書由来) |

## 11. 将来拡張（参考）

- 他ジョブ（データ追加のみで対応できる構造を維持）。モックでナイト（タンク）と白魔道士（ヒーラー）を試作済み: 技の共通の仕組み（時間・GCD・硬直・先行入力・詠唱・コンボ・ステータス）と、ジョブごとの決まり（`mock/jobs.js`: ボタンの変化・使える条件・効果・ゲージ）を分け、効果は説明文から読む（付与・発動条件・与ダメージ上昇・ヘイスト・回復・ゲージの消費）。役割ごとに相方（タンク / ヒーラー）が付く（DESIGN-06）
- ループ回し・任意長のお手本
- ダウンタイム・移動・ギミックのタイムライン（モックでは見下ろし 2D の練習場で、移動・方向指定・敵の範囲攻撃を試作済み）
- 威力データを用いた簡易ダメージ推定
- 入力ログのリプレイ再生（共有 URL から他人の入力を再生）
